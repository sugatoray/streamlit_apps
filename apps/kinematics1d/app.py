import streamlit as st
from textwrap import dedent

import utils as U

Defaults = U.Defaults

st.write("# Kinematics 1D ✨")

with st.sidebar:
    st.write("## ⚙️ Parameters")

    options = dict()

    options = st.text
    options["Dx"] = st.text_input(f"$\Delta x$ 👇", value="0.0")
    options["Dx"] = st.text_input(f"$\Delta x$ 👇", value="0.0")
    options["Dx"] = st.text_input(f"$\Delta x$ 👇", value="0.0")
    options["Dx"] = st.text_input(f"$\Delta x$ 👇", value="0.0")
    options["Dx"] = st.text_input(f"$\Delta x$ 👇", value="0.0")
    options["Dx"] = st.text_input(f"$\Delta x$ 👇", value="0.0")